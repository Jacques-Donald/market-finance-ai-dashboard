# ==============================================================================
# TEMPLATE DE CONFIGURATION DE L'ENVIRONNEMENT (POUR LE JURY)
# ==============================================================================

# ID du projet Google Cloud Console
PROJECT_ID=bigquery-finance-ia

# URL locale de l'API FastAPI (utilisée par le Dashboard Streamlit)
API_URL=http://127.0.0.1:8006

# ⚠️ INSTRUCTION SÉCURITÉ COMPTE DE SERVICE GCP :
# Pour faire fonctionner ce pipeline MLOps, générer une clé de compte de service
# au format JSON depuis la console Google Cloud (Rôles : BigQuery Admin / Vertex AI User).
# Renommeer ce fichier téléchargé en : "google_credentials.json"
# Le placer STRICTEMENT à la racine de ce projet.